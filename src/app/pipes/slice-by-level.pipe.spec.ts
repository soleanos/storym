import { SliceByLevelPipe } from './slice-by-level.pipe';

describe('SliceByLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceByLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
